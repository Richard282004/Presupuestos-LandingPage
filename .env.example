# No hay variables de entorno requeridas por ahora: la app es 100% estática
# y persiste todo en localStorage del navegador.
#
# Si en el futuro se agrega un backend o alguna API key, defínela aquí
# (sin valores reales) y léela en el código con import.meta.env.VITE_*
#
# VITE_API_URL=
